<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edz.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VariableActivity"
            android:label="@string/title_activity_variable"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ControlFlowActivity"
            android:label="@string/title_activity_control_flow"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OperatorActivity"
            android:label="@string/title_activity_operator"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>